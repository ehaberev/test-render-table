import getRow from 'test-render-table/utils/get-row';
import { module, test } from 'qunit';

module('Unit | Utility | get-row', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = getRow();
    assert.ok(result);
  });
});
